#include <bits/stdc++.h>

#define int int64_t
#define ii pair<int,int>
#define dd pair<double,double>
#define vi vector<int>
#define vvi vector<vi>
#define vii vector<ii>
#define vdd vector<dd>
#define pb push_back
#define ff first
#define ss second
#define mp make_pair
#define len(ar) (int)ar.size()
#define all(ar) ar.begin(), ar.end()
#define loop(i,s,r) for(int i=s; i<r; i++)
#define loopr(i,r,s) for(int i=r; i>=s; i--)
using namespace std;
const int MOD = 1e9+7;
const int INF = 1e18;
const int MAXN = 1e9+2;

inline void chkmax(int& a,int b) { a=max(a,b); }
inline void chkmin(int& a,int b) { a=min(a,b); }
inline void chkmax(double& a,double b) { a=max(a,b); }
inline void chkmin(double& a,double b) { a=min(a,b); }

//ifstream fin("input.in");
//ofstream fout("output.out");



void solve() {
	
}
/*

*/
int32_t main() {
	ios_base::sync_with_stdio(0); std::cin.tie(0);
	// int t; cin>>t; loop(i,0,t)
	loop(i,0,1)
		solve();
	return 0;
}
